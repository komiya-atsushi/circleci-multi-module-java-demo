test {
    useJUnitPlatform() {
        testLogging {
            showStandardStreams = true
            events "passed", "skipped", "failed"
        }
    }
}

release {
    tagTemplate = 'submodule-1-${version}'
    git {
        requireBranch = 'release-submodule-1'
    }
}
